import * as cut from './../../../src/lambda/store';

describe('store handler tests', () => {
    beforeEach(() => {
        jest.clearAllMocks();
    });

    it('should return status code 200', () => {
        
    })
})